import React from "react";

const Nav = () => (
  <nav className="navbar navbar-expand-lg navbar-dark bg-primary"style={{ justifyContent: "center"}}>
    <a className="navbar-brand" href="/">
      Star Books 
    </a>
  </nav>
);

export default Nav;
